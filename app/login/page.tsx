// app/login/page.tsx
"use client";

import { useState } from "react";
imp        <Lock className="w-4 h-4" />
             onClick={() => setShowPassword(!showPassword)}

          </p>
        </div>
      </div>
    </div>
  );
}